// make the DB from it later

export type Story = {
    id: string;
    founderName: string; 
    isAnonymous: boolean;
    storyTitle: string;
    industry: string;
    previewText: string;
    helpfulVotes: number;
    commentCount: number;
  };
  